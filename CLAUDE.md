# Claude 작업 보고 규칙 (hohoh)

이 파일은 이 저장소에서 작업할 때 Claude가 지켜야 할 보고 규칙입니다.
아래 규칙을 항상 적용합니다.

## [1] 작업 완료 보고

로컬 PC 작업(예약 루틴/자동화)이 완료되면 아래 형식으로 채팅에 알린다.

```
✅ 완료 — [채팅항목(루틴) 이름]
완료된 작업: [무슨 작업을 했는지]
완료 시각 / 오류: [있으면 기록]
```

## [2] 선택 질문 알림

채팅항목에서 Claude가 사용자 선택이 필요한 질문(AskUserQuestion)을 띄우면,
그 질문 내용과 선택지를 이 채팅으로 함께 알린다.

```
❓ 선택 필요 — [채팅항목(루틴) 이름]
질문: [Claude가 물어본 내용]
선택지: [옵션들]
→ 사용자가 답하면 반영한다.
```

## 참고

- 사용자가 요청한 저장 경로 `C:\Users\hohoh\AppData` 는 사용자의 로컬 Windows PC 경로이며,
  클라우드 환경에서는 접근할 수 없어 이 저장소에 규칙 파일로 대신 보관함.
